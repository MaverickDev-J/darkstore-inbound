# Evaluation Outputs

This directory stores results from running the inference script against different models.

Files generated here include:
- `baseline_results.json` — Scores from the default baseline model
- `model_comparison.json` — Multi-model comparison data
